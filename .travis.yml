language: python
python:
  - 2.7

install:
  - pip install -r requirements.txt
  - pip install coverage
  - python setup.py develop

script:
  - coverage erase
  - coverage run --source vixen -p -m nose.core -v vixen
  - coverage combine

after_success:
  - coverage report
